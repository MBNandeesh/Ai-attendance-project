# Backend
JWT_SECRET_KEY=change-me-to-a-32-plus-char-secret!!
# DATABASE_URL=postgresql+psycopg://user:pass@host/db   # Neon/other free Postgres in prod

# Frontend (NEXT_PUBLIC_ vars are baked at build time)
NEXT_PUBLIC_API_URL=http://localhost:8000
